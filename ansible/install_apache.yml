---
- name: Install Apache on AWS hosts
  hosts: aws_targets
  become: yes
  tasks:
    - name: Update packages
      ansible.builtin.apt:
        update_cache: yes
      when: ansible_os_family == "Debian"

    - name: Install Apache
      ansible.builtin.package:
        name: apache2
        state: present

